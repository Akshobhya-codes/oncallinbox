{
  "name": "oncall-inbox-voice",
  "version": "0.1.0",
  "description": "Voice + calling backbone for OncallInbox: Vapi calls the user about an urgent email, captures their spoken reply, drafts it with OpenAI, and sends it via AgentMail.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "tunnel": "echo \"Run: ngrok http %PORT% (or your tunnel) and put the https URL in PUBLIC_URL\"",
    "test:draft": "node scripts/test-draft.js",
    "test:trigger": "node scripts/test-trigger.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.67.3"
  }
}
